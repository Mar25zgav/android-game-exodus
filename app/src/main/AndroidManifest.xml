<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.exodus">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:fullBackupContent="true"
        android:supportsRtl="true">

        <activity
            android:name=".menupanel.MainActivity"
            android:screenOrientation="sensorLandscape"
            tools:ignore="LockedOrientationActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".menupanel.GameActivity"
            android:screenOrientation="sensorLandscape"
            tools:ignore="LockedOrientationActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
        </activity>

        <activity
            android:name=".menupanel.ShopActivity"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme.WindowPopUpTheme">
        </activity>

        <activity
            android:name=".menupanel.OptionsActivity"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme.WindowPopUpTheme">
        </activity>

        <activity
            android:name=".menupanel.ScoresActivity"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme.WindowPopUpTheme">
        </activity>

        <activity
            android:name=".menupanel.HelpActivity"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme.WindowPopUpTheme">
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>